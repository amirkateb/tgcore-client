{
  "name": "amirkateb/tgcore-client",
  "description": "TGCore Client is a lightweight Laravel package that receives Telegram updates forwarded by a TGCore gateway, verifies HMAC signatures with replay protection, normalizes payloads into a clean DTO, and dispatches them to your project handlers or queue with idempotency.",
  "type": "library",
  "license": "MIT",
  "require": {
    "php": "^8.1",
    "illuminate/support": "^10.0|^11.0",
    "illuminate/http": "^10.0|^11.0",
    "illuminate/routing": "^10.0|^11.0",
    "illuminate/database": "^10.0|^11.0",
    "illuminate/queue": "^10.0|^11.0",
    "illuminate/cache": "^10.0|^11.0"
  },
  "autoload": {
    "psr-4": {
      "amirkateb\\TGCoreClient\\": "src/"
    }
  },
  "extra": {
    "laravel": {
      "providers": [
        "amirkateb\\TGCoreClient\\TGCoreClientServiceProvider"
      ]
    }
  },
  "minimum-stability": "stable",
  "prefer-stable": true
}
